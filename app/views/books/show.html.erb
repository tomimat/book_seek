<% @book_tags.each do |tag| %>
  <%=tag.name%><%="(#{tag.books.count})" %><% end %>

   <!--タグ-->
  <% @book_tags.each do |tag| %>
  <%= link_to tag.name,search_tag_path(tag_id: tag.id)%><%="(#{tag.book.count})" %><% end %>

<div>
 <%= book_tag @book.get_image %>
  <p>タイトル：<%= @book.name %></p>
  <p>感想：<%= @book.caption %></p>
  <p>名前：<%= @book.user.name %></p>
  <p>投稿日：<%= @book.created_at.strftime('%Y/%m/%d') %></p>
</div>
